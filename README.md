# test-httptrig-function-app

Login to Azure:

```
az login
```

Deploy to Azure:

```
az group deployment create --name HackContrivedFunctionDeployment --resource-group Developers_group --template-file ./azure-deploy.json
```
